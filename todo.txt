# At initialization read actual position from driver chip. Actual code set to 0 actual position variable.

# Protect command that is in execution (do not let a new command to override it). To do this, a new command could be registered only if the sw motor controller is in Idle state (add this condition each time a command is written)

# Implement error handler because there are too many places where the error is to be generated.
